<p>Create new comment</p>

<%= form_for :comment, url: comments_path, remote: true do |f| %>
	<%= f.hidden_field :media_id, value: @medium.id %>
    <%= label_tag 'Text: ' %>
    <%= f.text_field :text %>
    <%= f.submit 'New comment' %>
<% end %>