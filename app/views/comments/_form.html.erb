<p>Create new comment</p>

<%= form_for :comment, url: medium_comments_path(medium_id: @medium.id), remote: true do |f| %>
    <%= label_tag 'Text: ' %>
    <%= f.text_field :text %>
    <%= f.submit 'New comment' %>
<% end %>